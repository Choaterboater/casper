/** Reported by `casper --version` and checked against package.json by the release build.
 * A compiled binary cannot read package.json, so the version has to live in code. */
export const CASPER_VERSION = "0.2.0";
